[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = [
    "E501",  # keep long command assertions readable
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["E501"]
